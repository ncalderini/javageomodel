apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'com.jfrog.bintray'

Properties properties = new Properties()
properties.load(project.rootProject.file('local.properties').newDataInputStream())
def bintrayUser = properties.getProperty('bintray.user')
def bintrayKey = properties.getProperty('bintray.apikey')

version '0.1'
group 'com.github.ncalderini'

bintray {
    user = bintrayUser
    key = bintrayKey
    configurations = ['archives']
    pkg {
        repo = 'maven'
        name = 'geocell'
        licenses = ['MIT']
        vcsUrl = 'https://github.com/ncalderini/javageomodel.git'
        version {
            name = '0.1'
        }
    }
}

dependencies {
    compile 'com.google.appengine:appengine-api-1.0-sdk:1.9.21'
    compile 'com.googlecode.objectify:objectify:5.1.13'
    compile 'commons-lang:commons-lang:2.4'
    compile 'javax.jdo:jdo2-api:2.3-20090302111651'
    compile 'org.apache.geronimo.specs:geronimo-jpa_3.0_spec:1.1.1'

    testCompile 'junit:junit:4.8.1'
    testCompile 'org.mockito:mockito-all:1.8.5'
}